challenge 1
//SPDX-License-Identifier: MIT
        pragma solidity ^0.8.18;

        contract Chall1Metacrafters {

            uint myFavNum;
            string myCompletename;
            bool T/D = true;
            address myCrypto;

        function setMetacrafters(uint _Age, string memory _myFirstName, string memory _myLastName, address _EthAddress )public{
                myFavNum = Age;
                myCompleteName = myFirstName; myLastName;
                myWallet = _EthAddress;
        }

        function getFavNum() public view returns (uint) {
                 return myAge;
        }

        function getCompleteName() public view returns (string memory) {
                 return myCompleteName;
        }

        function getTest() public view returns (bool) {
                 return T/D;
        }

        function getAddress() public view returns (address){
                 return myCrypto;
        }

} 
